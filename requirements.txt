# Core ML stack
pyspark==3.5.1
mlflow==2.14.1

# API
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.1
python-multipart==0.0.9

# Testing
httpx==0.27.0
pytest==8.2.0

# pandas is required internally by mlflow (model registry, search_runs).
# Our application code does NOT use pandas directly.
pandas==2.2.2
